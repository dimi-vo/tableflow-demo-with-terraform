# Local .terraform folder (contains providers and modules)
# This can be several hundred MBs and is easily recreated via 'terraform init'
.terraform/

# .tfstate files contain sensitive data and should be stored in a remote backend (S3, GCS, etc.)
# Never commit these to version control.
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files by default as they often contain secrets
# If you have public variables, you can use a specific name like 'public.tfvars' and white-list it
*.tfvars
*.tfvars.json

# Ignore override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore plan files (can contain sensitive info and are temporary)
*.tfplan

# Ignore this file
.terraform.lock.hcl